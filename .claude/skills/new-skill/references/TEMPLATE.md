---
name: skill-name
description: >-
  Что делает скилл в одном предложении. Использовать, когда пользователь просит
  <действие>, упоминает <ключевые слова> или работает с <объектами>. Не
  использовать для <похожие, но чужие задачи>.
---

# Skill Name

Одно-два предложения: какую задачу закрывает скилл и на каком результате
считается выполненным.

## Когда применять

- Пользователь просит ...
- В запросе встречается ...

Не применять, если ... — тогда подходит скилл `<другой-скилл>`.

## Что нужно на входе

- `<параметр>` — что это, где взять, что делать, если не задан.
- Переменные окружения: `<VAR>` (описание). Проверить наличие до начала работы.

## Порядок действий

1. Проверить входные данные; чего не хватает — спросить у пользователя.
2. <Шаг с конкретной командой>:
   ```bash
   <команда>
   ```
3. <Шаг обработки данных>.
4. Показать результат в формате из раздела ниже.

## Формат результата

```
<пример вывода>
```

## Крайние случаи

- <Ситуация> → <что делать>.
- Ошибка сети / пустой ответ → повторить до 3 раз, затем сообщить пользователю.

## Полезные файлы

- `references/<файл>.md` — <что там> (читать только при <условие>).
- `scripts/<файл>.py` — <что делает> (запуск: `python scripts/<файл>.py`).
